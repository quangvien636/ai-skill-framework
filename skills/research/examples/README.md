# Research Examples

- `representative.yaml` maps supplied evidence into a decision-oriented brief.
- `minimal.yaml` demonstrates a useful brief when no evidence is supplied.
- `contradictory.yaml` preserves disagreement and calibrated uncertainty.
- `invalid-refusal.yaml` rejects invented source identity and verification.

These examples describe behavioral expectations; they are not external-source
fixtures and do not execute research.
