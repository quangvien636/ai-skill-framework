# Workflow Contract Tests

The production Workflow is registered in the contract, IR, and graph fixture
manifests. This proves schema validity, Workflow IR construction, Skill
reference resolution, and compatible version selection.
