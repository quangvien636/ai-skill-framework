# Workflow Example

The example in the package README maps all eight Workflow inputs into the
Content Creation Skill. A successful execution returns `content-package` and
`quality-report`; unsupported claims or unresolved required Knowledge stop at
manual review.
