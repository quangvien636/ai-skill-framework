# Examples

`representative.yaml` records the canonical composite input, sequential batches,
explicit artifact transfers, and final output names. It is descriptive test
data and is never executed by repository validation.
