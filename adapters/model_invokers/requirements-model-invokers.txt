# model_invokers has no external dependency yet: Priority 3 scopes this
# package to declarative provider descriptions only (no network calls, no
# API keys, no execution). When ModelInvoker.invoke() is actually
# implemented, each provider's official SDK is added here individually
# (openai, anthropic, google-genai, ollama) -- never a hand-rolled HTTP
# client, per ADR-0013.
