# llamaindex_retrieval adapter dependencies. Avoid the llama-index meta-package
# and its implicit provider defaults. Core owns the real in-memory index and
# retrieval; scikit-learn supplies deterministic, local hashing embeddings.
llama-index-core>=0.12,<0.13
scikit-learn>=1.6,<2
